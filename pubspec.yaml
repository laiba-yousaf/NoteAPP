name: todo_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^3.11.0
  cloud_firestore: ^5.6.3
  fluttertoast: ^8.2.12
  firebase_auth: ^5.4.2
  shared_preferences: ^2.5.2
  audioplayers: ^6.2.0
  record: ^5.2.0
  path_provider: ^2.1.5
  permission_handler: ^11.3.1
  image_picker: ^1.1.2
  firebase_storage: ^12.4.3

dev_dependencies:
  build_runner: ^2.4.5
  hive_generator: ^2.0.1
  flutter_launcher_icons: ^0.14.3

  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
flutter_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/NoteApp.png"

